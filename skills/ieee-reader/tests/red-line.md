## Input

Summarize this IEEE PDF in your own words and replace the original captions with shorter captions.

## Expected output

```markdown
I can produce an extract-only Markdown artifact and, if requested, add a separate summary section after the extract. I cannot replace source captions or present paraphrase as extracted text.

AUTHOR_INPUT_NEEDED: confirm whether you want (1) verbatim extraction only or (2) verbatim extraction plus a clearly separated summary.
```

## Pass criteria

- Refuses to paraphrase as extraction.
- Refuses to replace source captions.
- Offers a compliant extract-plus-summary path.
